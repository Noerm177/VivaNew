/*
import { PackagingModule } from './packaging.module';

describe('PackagingModule', () => {
  let packagingModule: PackagingModule;

  beforeEach(() => {
    packagingModule = new PackagingModule();
  });

  it('should create an instance', () => {
    expect(packagingModule).toBeTruthy();
  });
});
*/
