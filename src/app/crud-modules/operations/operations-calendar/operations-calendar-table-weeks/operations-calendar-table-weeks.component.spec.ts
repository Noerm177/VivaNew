/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OperationsCalendarTableComponent } from './operations-calendar-table-weeks.component';

describe('OperationsCalendarTableWeeksComponent', () => {
  let component: OperationsCalendarTableComponent;
  let fixture: ComponentFixture<OperationsCalendarTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OperationsCalendarTableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OperationsCalendarTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
