/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OperationsCalendarHeaderComponent } from './operations-calendar-header.component';

describe('OperationsCalendarHeaderComponent', () => {
  let component: OperationsCalendarHeaderComponent;
  let fixture: ComponentFixture<OperationsCalendarHeaderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OperationsCalendarHeaderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OperationsCalendarHeaderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
