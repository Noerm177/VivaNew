/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OperationsCalendarTableDaysComponent } from './operations-calendar-table-days.component';

describe('OperationsCalendarTableDaysComponent', () => {
  let component: OperationsCalendarTableDaysComponent;
  let fixture: ComponentFixture<OperationsCalendarTableDaysComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OperationsCalendarTableDaysComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OperationsCalendarTableDaysComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
