/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OperationsCalendarTasksListComponent } from './operations-calendar-tasks-list.component';

describe('OperationsCalendarTasksListComponent', () => {
  let component: OperationsCalendarTasksListComponent;
  let fixture: ComponentFixture<OperationsCalendarTasksListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OperationsCalendarTasksListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OperationsCalendarTasksListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
